// Import React library to use React components
import React from 'react';

// Import ReactDOM library to render React components to the DOM
import ReactDOM from 'react-dom';

// Import the App component from the "./components/App" file
import App from "./components/App";

// Render the App component inside the element with the id "root" in the HTML DOM
ReactDOM.render(<App />, document.getElementById("root"));
